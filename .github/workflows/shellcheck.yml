name: shellcheck
on: push

jobs:
  shellcheck:
    name: shellcheck
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: run shellcheck
      uses: ludeeus/action-shellcheck@master
