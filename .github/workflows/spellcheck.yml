name: spellcheck

on:
  push:
    branches-ignore:
      - master
jobs:
  spellcheck:
    name: typo ci
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
    - name: spellcheck
      uses: typoci/spellcheck-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
